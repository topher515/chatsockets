Just start the Node server with `node chat.js`. The default
is to start the server at 'localhost:8030'.

Then open the `chat.html` in a webbrowser; the Javascript on 
`chat.html` attempts to open connections to the `chat.js`
server at 'localhost:8030'.
